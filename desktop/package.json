{
  "name": "pioneer-work-suite-desktop",
  "version": "0.1.12",
  "private": true,
  "scripts": {
    "tauri:dev": "node ../scripts/sync-version.mjs && tauri dev",
    "tauri:build": "node ../scripts/sync-version.mjs && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  }
}
